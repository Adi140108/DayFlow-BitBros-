rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default deny all access
    // This establishes a fail-closed security foundation.
    // Future business rules will be added here per module.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
